{
  "name": "familyos-extension",
  "version": "0.1.0",
  "private": true,
  "description": "FamilyOS Chrome extension (precision-first autofill)",
  "scripts": {
    "test": "node --test --experimental-test-isolation=none tests/unit/*.test.js"
  }
}
